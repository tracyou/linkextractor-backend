extend type Query {
    articlesByLaw(
        lawId: UUID! @rules(apply: ["exists:laws,id"]) @rename(attribute: "law_id")
    ): [Article!]!
}
type Article {
    id: UUID! @orderBy
    title: String!
    text: String!

    law: Law! @belongsTo
    annotations: [Annotation!]! @hasMany
}
